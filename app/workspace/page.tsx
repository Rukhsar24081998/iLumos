import { WorkspaceApp } from "@/components/workspace/WorkspaceApp";

/**
 * Screen 2 — AI Workspace.
 * Phase 3: interactive three-panel UI with mock data only.
 */
export default function WorkspacePage() {
  return <WorkspaceApp />;
}
